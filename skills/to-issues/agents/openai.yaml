interface:
  display_name: "To Issues"
  short_description: "Draft dependent issues from an approved PRD or plan"
  default_prompt: "Use $to-issues to decompose this approved PRD or plan into dependent issue drafts; publish only with explicit approval."
policy:
  allow_implicit_invocation: false
