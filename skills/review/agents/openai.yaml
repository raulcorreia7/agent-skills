interface:
  display_name: "Review"
  short_description: "Assess supplied changes, PRs, and designs"
  default_prompt: "Use $review to assess this supplied change, pull request, or design against its intent and available evidence."
policy:
  allow_implicit_invocation: false
