interface:
  display_name: "Worktrees"
  short_description: "Manage Git worktrees with safety checks"
  default_prompt: "Use $worktrees to carry out this Git worktree workflow with explicit safety checks."
policy:
  allow_implicit_invocation: false
