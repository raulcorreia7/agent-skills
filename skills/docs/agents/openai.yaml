interface:
  display_name: "Docs"
  short_description: "Create or review standalone docs, not comments or system bundles"
  default_prompt: "Use $docs for standalone documentation; exclude source comments and coordinated system bundles."
policy:
  allow_implicit_invocation: true
