interface:
  display_name: "Triage"
  short_description: "Order and route signals into owned next actions"
  default_prompt: "Use $triage to classify these signals, order them, and name the next action and owner for each."
policy:
  allow_implicit_invocation: false
