interface:
  display_name: "Adversarial Review"
  short_description: "Run two isolated reviews and adjudicate evidence"
  default_prompt: "Use $adversarial-review to run isolated breaker and verifier reviews of this artifact, then adjudicate their evidence."
policy:
  allow_implicit_invocation: false
